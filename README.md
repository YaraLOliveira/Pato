# Patinho de Bolso

Jogo web em pixel art com historinhas, reações e música.

O projeto é estático e não precisa de instalação nem de compilação. Para executar localmente, sirva a pasta `dist` em um servidor HTTP. A configuração `vercel.json` publica a pasta `dist` na Vercel.
